# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local
.env.*.local

# macOS-specific files
.DS_Store

# editors
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
.idea/

# astro
.astro/

# vercel
.vercel

# notes & planning (local only)
CLAUDE.md
TODO.md
NOTES.md
.notes/

# AI tooling — local by default; opt in specific shareable artifacts below.
#
# `.claude/skills/` is broadly tracked (production-skill convention from
# PR #104). New skills land in git unless explicitly re-ignored.
#
# `.claude/agent-verdicts/` is per-file because most entries are local-only
# reasoning (e.g. README-SYNC-AGENT.md). The skill-registry's verdict doc
# AND its dated registry reports (JSON for data + an optional MD writeup)
# are both tracked under the `SKILL-REGISTRY-*` pattern so other Claude
# instances can read the current portfolio inventory without re-running
# the scan. Other skills that should ship their docs add a
# `!.claude/agent-verdicts/<FILE>` line of their own.
#
# Everything else (settings, worktrees, transcripts) stays local.
.claude/*
!.claude/skills/
!.claude/agent-verdicts/
.claude/agent-verdicts/*
!.claude/agent-verdicts/SKILL-REGISTRY-*
.cursor/
.aider*
.continue/
.windsurf/
