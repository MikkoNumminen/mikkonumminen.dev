# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local
.env.*.local

# macOS-specific files
.DS_Store

# editors
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
.idea/

# astro
.astro/

# vercel
.vercel

# notes & planning (local only)
CLAUDE.md
TODO.md
NOTES.md
.notes/

# AI tooling — local by default; opt in specific shareable artifacts below.
#
# `.claude/skills/` is broadly tracked (production-skill convention from
# PR #104). New skills land in git unless explicitly re-ignored.
#
# `.claude/agent-verdicts/` is per-file because most entries are local-only
# reasoning (e.g. README-SYNC-AGENT.md) and the runtime outputs of skills
# that write into this directory should stay ignored. Add a new
# `!.claude/agent-verdicts/<FILE>.md` line for each verdict doc that
# should ship with its skill.
#
# Everything else (settings, worktrees, transcripts) stays local.
.claude/*
!.claude/skills/
!.claude/agent-verdicts/
.claude/agent-verdicts/*
!.claude/agent-verdicts/SKILL-REGISTRY-AGENT.md
.cursor/
.aider*
.continue/
.windsurf/
