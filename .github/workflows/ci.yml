name: CI

on:
  push:
    branches: [master]
  pull_request:

# Least privilege: the gate only reads the repo. No write scope on GITHUB_TOKEN.
permissions:
  contents: read

# Cancel a superseded run on the same ref so rapid pushes don't pile up
# redundant CI (and don't waste minutes racing each other).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run format:check
      - run: npm run lint
      - run: npm run test:coverage
      - run: npm run build

  # The RAG backend (chat-backend/) — Python — was never CI-checked, so latent
  # ruff/mypy nits stayed latent and the whole check rode on local runs + memory.
  # This job runs on every PR; to make it BLOCK merge, add `chat-backend` to the
  # branch-protection required status checks (Settings > Branches), same as `check`.
  chat-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: chat-backend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: chat-backend/pyproject.toml
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: mypy app evals
      - run: pytest
